// Enable Gradle toolchain auto-provisioning (Gradle 8.8+)
// This lets the build download a matching JDK (e.g., Java 22) if you don't have it installed locally.
plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}
